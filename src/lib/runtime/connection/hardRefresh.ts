/**
 * #1719 (Part of #1717) — Stage 2 hard refresh.
 *
 * RED stub — the GREEN commit implements the reconnect orchestration.
 */
export function useHardRefresh(): (connectionId: string) => Promise<void> {
  return async () => {};
}
