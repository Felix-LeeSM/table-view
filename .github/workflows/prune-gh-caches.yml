name: Prune GitHub Caches

on:
  schedule:
    - cron: "0 3 * * 1"
  workflow_dispatch:
    inputs:
      dryRun:
        description: "Run in dry-run mode"
        required: false
        default: "false"
        type: choice
        options:
          - "true"
          - "false"
      maxAgeDays:
        description: "Delete caches not accessed for at least this many days (backstop below GitHub's own 7-day eviction)"
        required: false
        default: "7"
        type: string
      minSizeGb:
        description: "Delete only caches larger than this size in GiB (0 = every size)"
        required: false
        default: "0"
        type: string

permissions:
  contents: read
  actions: write

jobs:
  prune:
    name: Remove stale caches
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Run cache prune script
        env:
          GH_TOKEN: ${{ github.token }}
          PRUNE_DRY_RUN: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.dryRun || 'false' }}
          PRUNE_MAX_AGE_DAYS: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.maxAgeDays || '7' }}
          PRUNE_MIN_SIZE_GB: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.minSizeGb || '0' }}
        run: |
          bash scripts/prune-gh-caches.sh
