# Development profile — daily dev workflow consumer.
# 5K-20K rows, 10-15% multilingual / edge mix.
seed: 4242
database:
  pg: table_view_development
  mongo: table_view_development
  # Sprint 281 — MySQL Slice A. PG / Mongo 와 같은 이름을 의도적으로
  # 사용 (시각적 일관성). MySQL 의 database = schema 라 별도 분리할 이유 없음.
  mysql: table_view_development
  # Sprint 452 — local SQLite file fixture. Resolved by fixture connection
  # upsert to app-data fixtures/sqlite/table_view_development.sqlite.
  sqlite: table_view_development.sqlite
locale_mix:
  en: 0.70
  ko: 0.10
  ja: 0.07
  zh: 0.05
  edge: 0.08
rows:
  customers: 5000
  products: 800
  orders: 12000
  order_items: 36000
  support_tickets: 2400
connections:
  pg:
    - id: fixture-dev-pg
      name: Dev (Postgres)
      color: "#7c3aed"
      environment: development
  mongo:
    - id: fixture-dev-mongo
      name: Dev (Mongo)
      color: "#16a34a"
      environment: development
  # Sprint 281 — MySQL Slice A. read path 만 동작 — query editor / DDL
  # 은 Slice B~D 이전엔 Unsupported 메시지가 surface 된다.
  mysql:
    - id: fixture-dev-mysql
      name: Dev (MySQL)
      color: "#0891b2"
      environment: development
  sqlite:
    - id: fixture-dev-sqlite
      name: Dev (SQLite)
      color: "#0891b2"
      environment: development
