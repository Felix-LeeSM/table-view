# E2E profile — compiled but currently dormant (tauri-driver cold-start
# / OOM blocks the WebDriver flow). Once unblocked, e2e specs run
# `db:reset e2e` per cycle; until then the profile is a static contract.
seed: 42
database:
  pg: table_view_e2e
  mongo: table_view_e2e
  # Sprint 281 — MySQL Slice A. e2e specs 는 현재 dormant (ADR 0019)
  # 라 본 fixture connection 은 화면 노출만 검증하는 용도.
  mysql: table_view_e2e
locale_mix:
  en: 0.95
  ko: 0.02
  ja: 0.01
  zh: 0.01
  edge: 0.01
rows:
  customers: 200
  products: 80
  orders: 500
  order_items: 1500
  support_tickets: 100
connections:
  pg:
    - id: fixture-e2e-pg
      name: E2E (Postgres)
      color: "#dc2626"
      environment: testing
  mongo:
    - id: fixture-e2e-mongo
      name: E2E (Mongo)
      color: "#dc2626"
      environment: testing
  mysql:
    - id: fixture-e2e-mysql
      name: E2E (MySQL)
      color: "#dc2626"
      environment: testing
