# H2 진행 기준

H2 RDBMS parity 는 **PostgreSQL lane 을 먼저 선택**했다. PostgreSQL 은 아직 가장
강한 full-parity 후보 lane 이고, MySQL/MariaDB 는 runtime smoke baseline 으로
승격돼 있다. MySQL catalog/workbench metadata 는 databases/schemas, tables, views,
columns, indexes, constraints/FKs, column CHECK hints 까지 live adapter
evidence 가 있다. MariaDB catalog/workbench metadata 는 tables/views/columns/indexes/
constraints/FKs/routine metadata browse 까지 MariaDB-specific Runtime Happy Path
probe 와 no-schema workbench coverage 로 닫혔고, CHECK hints 는 MariaDB
`>= 10.2.1` version context gate 를 유지한다. MySQL parser/Safe Mode 는 `LIMIT offset,count`,
`ON DUPLICATE KEY UPDATE`, narrow `CALL proc(scalar)` 와 명시 unsupported scripting
guard 까지 닫았고, MySQL/MariaDB runtime smoke 는 seeded narrow CALL result path 까지만 증명한다.
MySQL row-edit generated SQL 은 backtick quoting, primary-key
row projection, JSON/scalar/null coercion, preview/commit/discard consistency
까지 unit/hook evidence 가 있다. MariaDB row edit 는 MySQL-family generated SQL
경로를 MariaDB connection identity 로 검증한다. MySQL/MariaDB bounded structured
DDL 은 table/index/constraint preview/export evidence 와 bounded Structure
table/index/FK smoke evidence 로 제한되고, trigger create/drop 은 structured UI 에서 raw-SQL-only non-goal 로 남긴다.
MySQL completion context 는 current connection/database catalog 기반 editor
suggestion evidence 로 닫혔지만, completion runtime smoke 와 broader workbench,
admin/import/export parity 는 별도 promotion gate 로 남긴다.
MySQL/MariaDB/SQLite/DuckDB 를 full parity lane 으로 넓히려면
별도 runtime/parser/Safe Mode/completion/edit/fixture/E2E/support-claim gate 가 필요하다.
MariaDB 작업은 MariaDB engine baseline, catalog/workbench parity, delta hardening 으로 제한한다.
SQLite/DuckDB 작업은 현재 claim 정합성, typed capability gate,
fixture/smoke evidence routing 으로 제한한다.

## H2 umbrella gates

- [Active lane selection](#active-lane-selection)
- [RDBMS common smoke matrix](#rdbms-common-smoke-matrix)
- [MySQL version-aware capability gate](#mysql-version-aware-capability-gate)
- [MySQL row-edit generated SQL gate](#mysql-row-edit-generated-sql-gate)
- [MySQL bounded structure DDL gate](#mysql-bounded-structure-ddl-gate)
- [MySQL autocomplete context gate](#mysql-autocomplete-context-gate)
- [MySQL test coverage recheck](#mysql-test-coverage-recheck)
- [MariaDB engine baseline gate](#mariadb-engine-baseline-gate)
- [MariaDB catalog/workbench metadata gate](#mariadb-catalogworkbench-metadata-gate)
- [MariaDB row edit and bounded DDL parity gate](#mariadb-row-edit-and-bounded-ddl-parity-gate)
- [MariaDB delta/evidence gate](#mariadb-deltaevidence-gate)
- [MariaDB autocomplete delta gate](#mariadb-autocomplete-delta-gate)
- [MariaDB support-claim closure audit](#mariadb-support-claim-closure-audit)
- [MariaDB docs recheck](#mariadb-docs-recheck)
- [MariaDB test coverage recheck](#mariadb-test-coverage-recheck)
- [MySQL/MariaDB support claim SOT](#mysqlmariadb-support-claim-sot)
- [RDBMS docs-code consistency](#rdbms-docs-code-consistency)

### Active lane selection

**Current owner**: `docs/ROADMAP.md`, #185

**H2 boundary**: PostgreSQL first. Full admin parity is out of scope.

### RDBMS common smoke matrix

**Current owner**: `docs/contributor-guide/testing-and-quality.md`, #240

**H2 boundary**: Current remote E2E smoke proves PostgreSQL as the strongest
RDBMS lane and MySQL/MariaDB as narrower runtime baselines with bounded
Structure table/index/FK DDL smoke. Other RDBMS lanes use
unit/integration/fixture evidence until promoted.

### MySQL version-aware capability gate

**Current owner**: `src-tauri/src/db/mysql/version.rs`,
`src-tauri/tests/mysql_integration.rs`,
`src/types/dataSourceVersionCapabilities.ts`, `src/types/adapterConformance.ts`,
#221

**H2 boundary**: CHECK/constraint catalog metadata uses live server version
context and is enabled only at MySQL `>= 8.0.16` / MariaDB `>= 10.2.1`.

### MySQL row-edit generated SQL gate

**Current owner**: `src/components/datagrid/sqlGenerator.test.ts`,
`src/components/datagrid/useDataGridEdit.mixed-batch.test.ts`, #444

**H2 boundary**: MySQL row edits have dialect-specific identifier quoting,
primary-key row projection, JSON/scalar/null coverage, and
preview/commit/discard consistency evidence.

### MySQL bounded structure DDL gate

**Current owner**: `src-tauri/src/db/mysql/mutations.rs`,
`src-tauri/tests/mysql_integration.rs`,
`src/components/structure/IndexesEditor.test.tsx`,
`src/components/schema/StructurePanel.triggers.test.tsx`,
`e2e/smoke/mysql.spec.ts`, #445/#870

**H2 boundary**: Table/index/constraint structured DDL has preview/confirmation
evidence plus bounded Structure table/index/FK smoke evidence. MySQL trigger
metadata stays readable; structured trigger create/drop controls are hidden
because MySQL trigger bodies are raw-SQL-only in the current model.

### MySQL autocomplete context gate

**Current owner**:
`src-tauri/sql-parser-core/src/completion/completion_tests.rs`,
`src/features/completion/sql/sqlCompletionContext.test.ts`,
`src/lib/sql/sqlCompletionWasm.test.ts`,
`src/features/completion/sql/sqlHybridCompletionSource.test.ts`, #446

**H2 boundary**: MySQL completion uses the current connection/database catalog
for schema, table/view, column, and routine suggestions, including
schema-qualified prefixes and backtick identifier contexts. This is editor
completion evidence, not runtime support for stored routine bodies or scripting.

### MySQL test coverage recheck

**Current owner**: `docs/contributor-guide/testing-and-quality.md`,
`src-tauri/tests/mysql_integration.rs`, `src-tauri/tests/cancel_mysql.rs`,
`src-tauri/src/db/mysql/queries.rs`, `src-tauri/src/db/mysql/mutations.rs`,
`src-tauri/sql-parser-core/src/parser/tests.rs`,
`src/lib/sql/mysqlScriptingBoundary.test.ts`,
`src/components/query/QueryTab/useQueryExecution.test.tsx`,
`src/components/datagrid/sqlGenerator.test.ts`,
`src/components/datagrid/useDataGridEdit.mixed-batch.test.ts`,
`src/features/completion/sql/sqlCompletionContext.test.ts`,
`src/lib/sql/sqlCompletionWasm.test.ts`,
`src/features/completion/sql/sqlHybridCompletionSource.test.ts`,
`src-tauri/sql-parser-core/src/completion/completion_tests.rs`,
`scripts/e2e-smoke-ci.sh`, `e2e/smoke/mysql.spec.ts`,
`e2e/fixtures/mysql/query/seed.sql`, `scripts/fixtures/dbms-seeds.test.ts`,
#530/#872

**H2 boundary**: #530 maps backend runtime/query/catalog/cancel tests,
source-equivalent edit/DDL tests, parser/safety unsupported-boundary tests,
autocomplete vocabulary/context tests, and fixture/live smoke routing before
parity closure. #872 adds a seeded narrow CALL result path with scalar-argument
guardrails while keeping routine body authoring, broad CALL expressions, and
completion-runtime claims out of scope. Fixture-only and completion-only
evidence remain non-runtime support claims.

### MariaDB engine baseline gate

**Current owner**: `e2e/smoke/mariadb.spec.ts`,
`e2e/fixtures/mariadb/query/seed.sql`, `scripts/e2e-smoke-ci.sh`,
`.github/workflows/e2e-smoke.yml`, #448/#872

**H2 boundary**: MariaDB has distinct engine evidence for the routine
connect/browse/query/edit/cancel/history/result-envelope baseline and seeded
narrow CALL result path instead of inheriting the MySQL runtime-smoke claim.

### MariaDB catalog/workbench metadata gate

**Current owner**: `e2e/fixtures/mariadb/query/seed.sql`,
`e2e/smoke/mariadb.spec.ts`, `e2e/smoke/mysql-family-baseline.ts`,
`src/components/schema/SchemaTree.dbms-shape.test.tsx`,
`src/types/adapterConformance.test.ts`, `src-tauri/src/db/mysql/schema.rs`, #452

**H2 boundary**: MariaDB-specific smoke seed/category evidence covers tables,
views, columns, indexes, constraints/FKs, and routine metadata browse. CHECK
hints remain version-gated at MariaDB `>= 10.2.1`; procedure body
authoring/management, admin/import/export, and completion-runtime support remain
future gates.

### MariaDB row edit and bounded DDL parity gate

**Current owner**:
`src/components/datagrid/useDataGridEdit.mixed-batch.test.ts`,
`src/lib/sql/ddlGenerator.test.ts`, `src/lib/sql/ddlGenerator.ts`,
`src/hooks/useMigrationExport.ts`, `src-tauri/tests/mariadb_ddl_preview.rs`,
`e2e/smoke/mariadb.spec.ts`, #453/#870

**H2 boundary**: MariaDB row edits keep the MySQL-family quoted,
primary-key-projected preview/discard/commit path under MariaDB connection
identity. Bounded table/index/constraint DDL keeps MySQL-family SQL emission
while export headers preserve MariaDB identity, and the MariaDB engine smoke
proves bounded Structure table/index/FK preview/execute/catalog readback.
Trigger create/drop, procedure body management, admin, import/export, and full
vendor-workbench parity remain separate gates.

### MariaDB delta/evidence gate

**Current owner**: `src/types/dataSourceRuntime.ts`,
`src/lib/sql/sqlDialectProfile.ts`,
`src/features/completion/sql/sqlCompletionRequest.test.ts`,
`src/types/adapterConformance.ts`,
`src-tauri/sql-parser-core/src/parser/tests.rs`,
`src/lib/sql/sqlSafety.test.ts`, `src-tauri/tests/mariadb_returning_runtime.rs`,
#222/#450/#451/#871

**H2 boundary**: MariaDB keeps identity/profile and profile/completion
`RETURNING` delta. #451 decides not to add a client-side MariaDB `RETURNING`
runtime/version support gate; parser/Safe Mode recognition is structural and
keeps the normal DML safety tiers while raw execution remains server-resolved.
#871 adds focused `mariadb:11` runtime characterization: the current server
accepts `DELETE ... RETURNING` and applies the delete, while the shared adapter
exposes no returned rows and no affected-row count. The MySQL-family reuse audit
lists intentional shared adapter/parser/completion/capability paths and keeps
MySQL-only evidence from promoting MariaDB claims without MariaDB-specific
tests/docs.

### MariaDB autocomplete delta gate

**Current owner**: `src/features/completion/sql/sqlCompletionRequest.ts`,
`src/lib/sql/sqlCompletionWasm.ts`,
`src-tauri/sql-parser-core/src/completion/**`,
`src/features/completion/sql/sqlCodeMirrorCompletionAdapter.test.ts`, #454

**H2 boundary**: MariaDB completion requests keep distinct `mariadb` identity
while reusing the MySQL family and `mysql-client` shell. Known MariaDB versions
below `10.0.5` suppress the `RETURNING` suggestion; unknown versions and
`>= 10.0.5` keep the keyword-level profile delta. This is completion assistance
only, not a statement-specific runtime support guarantee.

### MariaDB support-claim closure audit

**Current owner**: `docs/product/README.md`,
`docs/product/query-language-support.md`, `docs/product/known-limitations.md`,
`docs/contributor-guide/testing-and-quality.md`, #455

**H2 boundary**: Product docs now separate MariaDB from MySQL by naming MariaDB
engine smoke, catalog/workbench metadata, row-edit/DDL, parser/Safe Mode,
autocomplete, and unsupported promotion slices independently. The testing matrix
names MariaDB-specific engine evidence and below-smoke focused evidence while
keeping fixture-only and completion-only evidence out of
runtime/admin/import/export claims.

### MariaDB docs recheck

**Current owner**: `docs/product/README.md`,
`docs/product/query-language-support.md`, `docs/product/known-limitations.md`,
`docs/contributor-guide/testing-and-quality.md`, #531

**H2 boundary**: Final docs recheck confirms the product snapshot, SQL support
page, known limitations, and testing matrix match shipped MariaDB behavior.
Runtime smoke, parser/Safe Mode, autocomplete, below-smoke focused tests, and
future unsupported promotion slices remain separated; fixture-only evidence is
not described as live runtime support.

### MariaDB test coverage recheck

**Current owner**: `docs/contributor-guide/testing-and-quality.md`,
`e2e/smoke/mariadb.spec.ts`, `src-tauri/tests/mariadb_ddl_preview.rs`,
`src-tauri/sql-parser-core/src/parser/tests.rs`,
`src/features/completion/sql/sqlCompletionRequest.test.ts`, #532

**H2 boundary**: Final test recheck maps MariaDB runtime/query/edit evidence,
source-equivalent row-edit/DDL tests, parser/Safe Mode and unsupported-boundary
tests, autocomplete vocabulary/context tests, fixture/live evidence, and smoke
routing before milestone closure.

### MySQL/MariaDB support claim SOT

**Current owner**: `docs/product/README.md`,
`docs/product/known-limitations.md`, `docs/product/query-language-support.md`,
#207/#443/#447/#448/#529/#530

**H2 boundary**: Shared MySQL-family behavior, explicit unsupported scripting
boundaries, MariaDB-specific engine baseline/deltas, MySQL closure-claim
boundaries, and final MySQL docs/test recheck boundaries are separated.
Fixture-only inventory, completion suggestions, and below-smoke tests do not
widen runtime/admin/import/export claims.

### RDBMS docs-code consistency

**Current owner**: `docs/product/**`, #198

**H2 boundary**: Product-visible claims stay narrower than implemented/evidenced
behavior.

## H2 umbrella closure

H2 umbrella closure does not mean every RDBMS has full desktop-client parity. It
means the current support claims, capability gates, and smoke routing are aligned
so the next implementation lane can proceed without widening unsupported claims.

## PostgreSQL query/workbench parity lane

PostgreSQL lane closure for #186/#241 means the current claim, smoke, and gap
inventory is explicit enough to choose the next implementation slice. It does
not mean Table View has full PostgreSQL admin parity, arbitrary dialect
semantics, or a broad desktop E2E suite.

- [Runtime execution](#runtime-execution)
- [Catalog/workbench](#catalogworkbench)
- [Parser and Safe Mode](#parser-and-safe-mode)
- [Completion and extensions](#completion-and-extensions)
- [Edit semantics](#edit-semantics)
- [Lightweight Explain](#lightweight-explain)
- [Cancellation workflow](#cancellation-workflow)
- [Fixture and E2E smoke](#fixture-and-e2e-smoke)
- [Test coverage closure baseline](#test-coverage-closure-baseline)
- [Support claim SOT](#support-claim-sot)

### Runtime execution

**Current owner**: `src-tauri/src/db/postgres/queries.rs`,
`src-tauri/src/db/postgres/schema.rs`, `src-tauri/tests/query_integration.rs`,
`src-tauri/tests/cancel_pg.rs`

**PostgreSQL boundary**: Connection, table data, raw SELECT/EXPLAIN row results,
plan-only `EXPLAIN (FORMAT JSON)`, DML batches, query cancellation, and
raw-query grid edit paths are active. psql meta-command execution, DB-level
backup/restore/import/export, and PL/pgSQL body authoring are not parity claims.

### Catalog/workbench

**Current owner**: `src-tauri/src/db/postgres/schema.rs`,
`src-tauri/tests/schema_integration.rs`, `src/components/schema/**`,
`src/components/rdb/**`

**PostgreSQL boundary**: Schemas, tables, views, functions, types, installed
extensions, triggers, table stats, indexes, constraints, FKs, cached metadata,
DataGrid, Structure, ERD inputs, migration impact summaries, and cached
read-only schema diff have current evidence. Server activity, profiler, full
stats dashboards, role/user/permission UI, extension management UI, data
compare, and migration/apply execution stay future work.

### Parser and Safe Mode

**Current owner**: `src-tauri/sql-parser-core/**`,
`src/lib/sql/sqlSafety.test.ts`,
`src/components/query/QueryTab.safe-mode.test.tsx`,
`src/components/query/QueryTab.warn-dialog.test.tsx`,
`src/components/datagrid/useDataGridEdit.safe-mode.test.ts`,
`e2e/smoke/postgres-safe-mode.spec.ts`

**PostgreSQL boundary**: The current subset classifies tested SQL slices,
destructive/warn/info statements, bounded writes, raw query confirmations, grid
edit confirmations, DDL preview, and EXPLAIN inner statements. Routine desktop
smoke now covers PostgreSQL info/warn/destructive confirmation, raw DDL
preview/confirm, and grid-edit preview/confirm paths. Full PL/pgSQL bodies,
broad MERGE variants, arbitrary function-expression semantics, and arbitrary
extension semantics are not modeled.

### Completion and extensions

**Current owner**: `src-tauri/src/db/postgres/schema.rs`,
`src/features/completion/sql/sqlCompletionContext.ts`,
`src/lib/sql/sqlCompletionWasm.test.ts`,
`src-tauri/sql-parser-core/src/completion/**`,
`e2e/smoke/postgres-extension-completion.spec.ts`

**PostgreSQL boundary**: Installed extension inventory is fetched and passed to
completion before curated known extension packs are enabled. Runtime smoke seeds
`pgcrypto` and `fuzzystrmatch`, proves `pg_extension` detection, surfaces
`GEN_RANDOM_UUID` and `LEVENSHTEIN`, withholds absent `uuid-ossp` candidates,
and keeps unknown installed extensions detected-but-unpacked. Completion must
not invent extension symbols or imply parser/Safe Mode semantic validation.

### Edit semantics

**Current owner**: `src/components/datagrid/**`,
`src/components/query/EditableQueryResultGrid*`,
`src/components/datagrid/sqlGenerator.test.ts`,
`src-tauri/src/db/postgres/queries.rs`

**PostgreSQL boundary**: Row edits require key/projected identity and keep
preview/commit/discard plus Safe Mode confirmation paths. Arbitrary query result
mutation, bulk admin workflows, and full desktop-client edit parity stay future
work.

### Lightweight Explain

**Current owner**: `src-tauri/src/db/postgres/schema.rs`,
`src/lib/api/explain.ts`, `src/components/query/ExplainViewer.test.tsx`,
`src/components/query/QueryTab.toolbar.test.tsx`, `src/lib/sql/sqlAst.test.ts`,
`e2e/smoke/postgres-explain.spec.ts`

**PostgreSQL boundary**: Lightweight plan inspection exists through backend,
API, parser, safety, component, and routine desktop smoke evidence. It is not a
full profiler/activity dashboard.

### Cancellation workflow

**Current owner**: `src-tauri/tests/cancel_pg.rs`,
`src/components/query/QueryTab.execution.test.tsx`,
`src/hooks/useQueryHistory.event-refetch.test.ts`,
`e2e/smoke/postgres-cancellation.spec.ts`

**PostgreSQL boundary**: Query toolbar cancellation reaches the backend token,
renders a cancelled state without stale grid results, records cancelled tab
history, and supports a clean retry. It is not a server activity/session
dashboard or arbitrary backend session-management claim.

### Fixture and E2E smoke

**Current owner**: `e2e/fixtures/postgresql/query/seed.sql`,
`e2e/smoke/postgres.spec.ts`, `e2e/smoke/postgres-safe-mode.spec.ts`,
`e2e/smoke/postgres-explain.spec.ts`,
`e2e/smoke/postgres-extension-completion.spec.ts`,
`e2e/smoke/postgres-cancellation.spec.ts`,
`e2e/smoke/postgres-structure-ddl.spec.ts`, `e2e/smoke/erd-dense.spec.ts`,
`scripts/e2e-smoke-ci.sh`

**PostgreSQL boundary**: Runtime Happy Path currently proves PostgreSQL connect
-> browse seeded `users` -> edit -> query result, Safe Mode
info/warn/destructive confirmation, raw DDL preview, grid-edit confirmation,
Explain plan-inspection/source-label, installed-extension-gated completion for
seeded `pgcrypto` and `fuzzystrmatch`, query cancellation UI/history/retry
behavior, bounded Structure table-plus-index DDL
preview/execute/history/schema-refresh behavior, and dense ERD
render/search/selection/zoom/fit desktop+narrow screenshot evidence on Ubuntu.
It does not cover broader structured DDL flows, broader history-source labeling,
FK row navigation through ERD, schema diff, migration impact, data compare,
admin, arbitrary extension semantics, or profiler/activity scenarios.

### Test coverage closure baseline

**Current owner**: `docs/contributor-guide/testing-and-quality.md`,
`src-tauri/tests/query_integration.rs`, `src-tauri/tests/schema_integration.rs`,
`src-tauri/tests/cancel_pg.rs`, `src-tauri/src/commands/rdb/query.rs`,
`src/lib/sql/sqlSafety.test.ts`,
`src/features/completion/sql/sqlCompletionContext.test.ts`,
`src/features/completion/sql/sqlCompletionRequest.test.ts`,
`src/lib/sql/sqlCompletionWasm.test.ts`,
`src-tauri/sql-parser-core/src/completion/completion_tests.rs`

**PostgreSQL boundary**: #528 recheck maps runtime/query/edit or
source-equivalent backend tests, parser/safety and unsupported-boundary tests,
autocomplete vocabulary/context tests, fixture/live evidence, and smoke routing.
Fixture-only evidence remains non-runtime evidence until the CI smoke script
wires it.

### Support claim SOT

**Current owner**: `docs/product/README.md`,
`docs/product/query-language-support.md`, `docs/product/known-limitations.md`

**PostgreSQL boundary**: Product-visible PostgreSQL claims must stay narrower
than the evidence above until new implementation slices add matching tests and
smoke routing.

## SQLite file DBMS parity lane

SQLite lane closure for #196/#242 means the file-DBMS write/parity,
extension-boundary, and smoke inventory is explicit. #874 adds a bounded
structured `CREATE TABLE` first slice for writable SQLite files. It does not
mean SQLite has raw SQL DDL, broader structured DDL parity, automatic ALTER
rebuilds, table/index removal or rename, index creation, standalone constraint
changes, extension capability semantics, sqlite-cli execution, or broader
desktop scenarios beyond the wired deterministic Runtime Happy Path file smoke.

- [File connection contract](#file-connection-contract)
- [Runtime query/write path](#runtime-querywrite-path)
- [Catalog/workbench](#catalogworkbench-1)
- [Edit semantics](#edit-semantics-1)
- [DDL and ALTER rebuild](#ddl-and-alter-rebuild)
- [Parser, Safe Mode, and completion](#parser-safe-mode-and-completion)
- [Fixture and smoke](#fixture-and-smoke)
- [Documentation recheck](#documentation-recheck)
- [Test coverage recheck](#test-coverage-recheck)
- [Support-claim closure audit](#support-claim-closure-audit)
- [DuckDB separation](#duckdb-separation)

### File connection contract

**Current owner**: `src/types/dataSource.ts`, `src/types/dataSource.test.ts`,
`src-tauri/src/db/adapters/sqlite/connection.rs`,
`src-tauri/tests/sqlite_connection_command.rs`,
`src/components/connection/forms/SqliteFormFields.test.tsx`

**SQLite boundary**: SQLite is a file-backed RDBMS with absolute-path
validation, create-new-file support, read-only mode, and internal app-state DB
rejection. It has no server host/auth, switch-database, or multi-namespace
parity claim.

### Runtime query/write path

**Current owner**: `src-tauri/src/db/adapters/sqlite/queries.rs`,
`src-tauri/src/db/adapters/sqlite/batch.rs`,
`src-tauri/src/db/adapters/sqlite/queries_tests.rs`,
`src-tauri/src/db/adapters/sqlite/batch_tests.rs`,
`src-tauri/tests/sqlite_browse_query_adapter.rs`, #458/#874

**SQLite boundary**: Read queries and writable-file DML run through the SQLite
adapter. Multi-statement DML batches are transactional, dry-run rolls back,
cancellation is wired, and read-only files reject writes. Raw SQL DDL is
adapter-rejected; structured table creation uses its own bounded DDL path for
writable files only.

### Catalog/workbench

**Current owner**: `src-tauri/src/db/adapters/sqlite/connection.rs`,
`src-tauri/src/db/adapters/sqlite/connection_tests.rs`,
`src-tauri/tests/sqlite_browse_query_adapter.rs`,
`src/components/schema/SchemaTree.dbms-shape.test.tsx`,
`src/components/schema/SchemaTree.rowcount.test.tsx`, #457/#1069

**SQLite boundary**: Current catalog evidence covers `main`, flat table
browsing, exact row counts, columns, FKs, indexes, views, view columns, and
read-only trigger listing/source from `sqlite_schema`. Schemas, stored functions
(SQLite has none — built-in and extension functions are engine primitives, not
surfaced as schema objects), full constraints, table stats parity, and richer
admin/workbench surfaces are not claimed.

### Edit semantics

**Current owner**: `src/types/dataSource.ts`,
`src/components/datagrid/sqlGenerator.test.ts`,
`src/components/datagrid/useDataGridEdit.safe-mode.test.ts`,
`src-tauri/src/db/adapters/sqlite/queries.rs`,
`src-tauri/src/db/adapters/sqlite/connection_tests.rs`,
`src-tauri/src/db/adapters/sqlite/queries_tests.rs`,
`src-tauri/src/db/adapters/sqlite/batch_tests.rs`, #459

**SQLite boundary**: Row edit support is scoped to writable files and
key/projected row identity. SQLite identifier quoting, scalar row writes,
preview/commit/discard confirmation paths, pending edit errors, and read-only
write rejection have evidence; nested JSON edits, arbitrary query-result
mutation, and bulk/admin edit workflows are outside current support.

### DDL and ALTER rebuild

**Current owner**: `src-tauri/src/db/adapters/sqlite/ddl.rs`,
`src-tauri/src/db/adapters/sqlite/mod.rs`,
`src-tauri/src/db/adapters/sqlite/queries_tests.rs`,
`src-tauri/src/db/adapters/sqlite/batch_tests.rs`,
`src-tauri/tests/sqlite_browse_query_adapter.rs`,
`docs/product/known-limitations.md`, #874

**SQLite boundary**: Structured DDL is limited to `CREATE TABLE` preview/execute
for writable SQLite files, with schema refresh proof. `create_table_plan`
rejects indexes and standalone constraints before creating the table. Raw SQL
DDL, ALTER rebuilds, table/index removal or rename, index creation, standalone
constraint changes, comments, identity columns, nested JSON edit, and extension
semantics remain unsupported. Automatic rebuild strategy requires a future
ADR-backed implementation slice.

### Parser, Safe Mode, and completion

**Current owner**:
`src-tauri/sql-parser-core/src/completion/completion_tests.rs`,
`src/features/completion/sql/sqlCompletionContext.test.ts`,
`src/features/completion/sql/sqlCompletionRequest.test.ts`,
`src/lib/sql/sqlSafety.test.ts`, `docs/product/query-language-support.md`,
#461/#873

**SQLite boundary**: Completion exposes SQLite keywords/functions, cached
table/column catalog context, sqlite-cli dot-command vocabulary as
non-executable suggestions, and detected-only JSON1/FTS5 read-query assistance
from a probed JSON1/FTS5/RTREE capability inventory. Parser/Safe Mode remain
bounded SQL support; sqlite-cli dot commands and `load_extension()` are not
executed, virtual-table CRUD and broad extension semantics are unsupported, and
capability inventory is not parser/Safe Mode validation.

### Fixture and smoke

**Current owner**: `e2e/fixtures/sqlite/query/seed.sql`,
`e2e/smoke/sqlite.spec.ts`, `scripts/fixtures/**`,
`scripts/e2e-pre-smoke-release-gate.ts`, `scripts/e2e-smoke-ci.sh`, #456/#874

**SQLite boundary**: SQLite has deterministic fixture files and a wired Runtime
Happy Path desktop smoke for file create/open, table browse, read query,
writable DML, row edit, bounded structured table creation with schema refresh
proof, read-only write rejection, and internal app-state DB rejection. Broader
DDL and extension-boundary claims remain separate gates.

### Documentation recheck

**Current owner**: `docs/product/README.md`,
`docs/product/query-language-support.md`, `docs/product/known-limitations.md`,
`docs/contributor-guide/testing-and-quality.md`, #533

**SQLite boundary**: Final docs recheck confirms the product snapshot, SQL
support page, known limitations, and testing matrix match shipped SQLite
behavior. Runtime smoke, parser/Safe Mode, autocomplete, adapter-level DDL
rejection, extension-boundary, and fixture-only evidence remain separated before
parity closure.

### Test coverage recheck

**Current owner**: `docs/contributor-guide/testing-and-quality.md`,
`src-tauri/tests/sqlite_connection_command.rs`,
`src-tauri/tests/sqlite_browse_query_adapter.rs`,
`src-tauri/src/db/adapters/sqlite/queries_tests.rs`,
`src-tauri/src/db/adapters/sqlite/batch_tests.rs`,
`src-tauri/sql-parser-core/src/completion/completion_tests.rs`,
`src/features/completion/sql/sqlCompletionContext.test.ts`,
`src/features/completion/sql/sqlCompletionRequest.test.ts`,
`src/lib/sql/sqlWasmArtifact.test.ts`, `scripts/e2e-smoke-ci.sh`,
`e2e/smoke/sqlite.spec.ts`, #534

**SQLite boundary**: Final test recheck maps runtime/query/edit backend
coverage, source-equivalent row-edit and unsupported-boundary coverage, shared
parser/Safe Mode coverage, autocomplete vocabulary/context coverage,
fixture/live evidence, and smoke routing before parity closure.

### Support-claim closure audit

**Current owner**: `docs/product/README.md`,
`docs/product/query-language-support.md`, `docs/product/known-limitations.md`,
`docs/contributor-guide/testing-and-quality.md`, #462

**SQLite boundary**: Final support-claim audit confirms README, query-language
support, known limitations, and testing matrix agree on the file-backed SQLite
DBMS claim. DuckDB/file analytics remains separate; fixture-only inventory,
completion-only evidence, structured DDL/admin/vendor CLI parity, and
unsupported dialect/runtime behavior do not become support claims.

### DuckDB separation

**Current owner**: `docs/ROADMAP.md`, `docs/product/query-language-support.md`,
`docs/product/known-limitations.md`

**SQLite boundary**: DuckDB/file analytics stays in the H3 lane. SQLite parity
work must not widen DuckDB file analytics claims.
