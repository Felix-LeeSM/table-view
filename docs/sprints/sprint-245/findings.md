# Sprint 245 Evaluation — ADR 0022 Phase 1 (Safe Mode policy 원복 + mode 재정의)

## Sprint 245 Evaluation Scorecard

| Dimension | Score | Notes |
|-----------|-------|-------|
| Correctness | 9/10 | `decideSafeModeAction` (`src/lib/safeMode.ts:54-99`) maps every cell of the contract's 8-case matrix exactly: non-prod+strict+danger → confirm with M.1 suffix copy; non-prod+strict+safe/read → allow; non-prod+warn/off+\* → allow; prod+\*+safe/read → allow; prod+strict|warn+danger → confirm bare reason; prod+off+danger → confirm with prod-auto suffix. Reason copies are coherent and mode-distinguishable: `(Safe Mode strict — destructive statement in non-production)` for non-prod strict vs. bare for prod+strict vs. `(production environment forces Safe Mode — change connection environment tag to override)` for prod+off. Empty-reason fallback (`"Dangerous statement"`) is pinned by `src/lib/safeMode.test.ts:191-204`. Generator's deviation from contract `AC-245-L6` ("strict/warn 은 toolbar override copy") to bare analyzer reason is **explicitly justified** in handoff Assumptions (Phase 1 dialog uses type-to-confirm; existing `[AC-186-04a]`, `[AC-186-05b]`, `[AC-231-02b]` rely on bare reason for typed input). The justification is sound — the contract Invariant explicitly states "현재 텍스트 그대로 유지" so preserving bare reason is the safer interpretation. -1 only because Generator didn't add an explicit `AC-245-L6` lib-level pin asserting "strict/warn share the bare copy" (the matrix tests cover it but the override-copy contract sentence reads ambiguously). |
| Completeness | 8/10 | All 8 Done Criteria from the execution-brief are addressed. Sprint 244 invert AC IDs (`AC-244-01..14`) are properly removed/re-IDed (no live re-use; only archaeology comments). 5 production files match the In Scope list exactly. 18 test files were touched — extras (`StructurePanel.{columns,constraints,indexes}.test.tsx`, `QueryTab.execution.test.tsx`, `QueryTab.document.test.tsx`, `IndexesEditor.test.tsx`, `ConstraintsEditor.test.tsx`, `ColumnsEditor.test.tsx`, `CreateTableDialog.test.tsx`, `DropTableDialog.test.tsx`, `DropColumnDialog.test.tsx`) are out-of-the-letter-of-In-Scope but justified — they previously assumed Sprint 244 read-only block paths and would have failed under the new policy. Generator pinned them to mode=warn or inverted assertions to keep the test suite green; this is *required* scope rather than scope creep. -2 because `AC-245-G1`, `AC-245-G2`, `AC-245-G3`, `AC-245-C2`, `AC-245-C5`, `AC-245-H1`, `AC-245-N1`, `AC-245-N2` IDs from the contract are not all pinned with their literal IDs in test code — `[AC-245-N1/N2]` appear correctly, but `G1/G2/G3` (DataGrid-level) and `C2/C5` (commit-preview destructive-still-blocks-via-confirm) are folded into pre-existing `[AC-185-06]`, `[AC-185-04a]`, `[AC-231-01a]`, `[AC-231-01c]` IDs with header comments explaining the inversion. The intent is preserved but the AC-ID-to-test-name mapping isn't 1:1, so `git grep AC-245-G1` returns no hits. |
| Reliability | 9/10 | Pure function `decideSafeModeAction` has no race conditions / stale state — the hook (`useSafeModeGate.ts:38-52`) re-derives via `useCallback` on `[mode, environment]` so prop drift propagates correctly (verified by `[AC-245-H2]` triple). Empty-reason fallback exercised. PR-prod-auto reason copy is **preserved verbatim** at `safeMode.ts:77` (`"... (production environment forces Safe Mode — change connection environment tag to override)"`); downstream test `useDataGridEdit.safe-mode.test.ts:215` and `EditableQueryResultGrid.safe-mode.test.tsx:223` regex-match this string so future drift will fail tests. M.1 non-prod strict reason ends with `"(Safe Mode strict — destructive statement in non-production)"` — distinct from prod+strict (bare) and prod+off (parenthesised override hint). Invariants preserved: prod+warn+destructive→confirm pinned at `safeMode.test.ts:135`; non-prod+warn/off→allow pinned at `safeMode.test.ts:92, 111`. -1: `useSafeModeReadOnly` cleanup is via grep (no live import), but `[AC-245-H1]` is not literally pinned as a test name — only verified by tsc + comments at `useSafeModeGate.test.ts:11-16` and `DataGrid.tsx:257-263`. |
| Verification Quality | 8/10 | All 5 required checks documented as passing. Spot-re-ran `safeMode.test.ts + useSafeModeGate.test.ts + SafeModeToggle.test.tsx` (3 files / 22 tests / 1.01s) and `useDataGridEdit.safe-mode.test.ts + EditableQueryResultGrid.safe-mode.test.tsx + QueryTab.safe-mode.test.tsx` (3 files / 29 tests / 1.09s) — both pass. Inverted-test descriptions are updated faithfully (no "block" descriptions paired with "confirm" assertions): `[AC-185-04a]` description now reads "confirm dialog, executeQueryBatch not called"; `[AC-231-01a]` reads "confirm dialog, executeQuery NOT called". TDD canary intact — `[AC-231-01a]` retained as canary (handoff Residual Risk note + comment at `QueryTab.safe-mode.test.tsx:166-173`) and would fail against pre-Sprint-245 code (which would have called executeQuery directly under the previous block path). Sprint 244 AC IDs only persist in archaeology header comments — `grep "AC-244-09\|AC-244-10\|...\|AC-244-14"` returns 0 live `it()` titles. -2 because the contract's `AC-245-G1..G3 / C2 / C5 / H1 / N1 / N2` AC IDs aren't all individually surfaced as test names — Generator chose to keep the historical AC IDs (`[AC-185-06]`, `[AC-231-01a]`, etc.) and inverted their assertions, which is faithful to git archaeology but makes contract-to-code mapping rely on header comments rather than `grep AC-245-X`. |
| **Overall** | **8.5/10** | All dimensions ≥ 7 — PASS. |

## Verdict: PASS

## Sprint Contract Status (Done Criteria)

- [x] **Criterion 1** — `decideSafeModeAction` matrix 8 cases — `src/lib/safeMode.ts:54-99` body, verified by `src/lib/safeMode.test.ts:55-205` (`[AC-245-L1..L8]` + empty-reason fallback case). Re-ran tests: 13 pass.
- [x] **Criterion 2** — `useSafeModeReadOnly` symbol absent — verified via `grep -rn useSafeModeReadOnly src/` returning only 2 archaeology-comment hits at `DataGrid.tsx:257` and `useSafeModeGate.test.ts:11`. Hook source at `src/hooks/useSafeModeGate.ts:32-52` exports only `SafeModeDecision`, `SafeModeGate`, `useSafeModeGate`.
- [x] **Criterion 3** — `DataGridToolbar.readOnly` prop absent — `src/components/datagrid/DataGridToolbar.tsx:39-88` interface contains no `readOnly` field; `grep readOnly src/components/datagrid/DataGridToolbar.tsx` returns 0 hits.
- [x] **Criterion 4** — DataGrid cell-edit / Add / Delete / Duplicate work in production+strict — verified by `[AC-185-06]` test at `DataGrid.editing.test.tsx:734` (Sprint 243 mode=warn workaround removed at lines 740-743 — cell-edit now flows under default strict mode). Toolbar handlers wired raw at `DataGrid.tsx:404-409` (no `disabled={readOnly}` branches).
- [x] **Criterion 5** — prod+strict + safe DML / safe write passes through — `[AC-245-C1]` at `useDataGridEdit.safe-mode.test.ts:136` (DELETE WHERE pk → executeQueryBatch ×1); `[AC-245-C3]` at `EditableQueryResultGrid.safe-mode.test.tsx:177` (UPDATE WHERE pk → executeQueryBatch ×1); `[AC-245-C4 / C4-2 / C4-3]` at `QueryTab.safe-mode.test.tsx:222, 242, 258` (INSERT / UPDATE WHERE / CREATE TABLE → executeQuery ×1).
- [x] **Criterion 6** — non-prod + strict + DROP TABLE → confirm dialog (M.1) — `[AC-245-N1]` at `QueryTab.safe-mode.test.tsx:278`; paired pass-through `[AC-245-N2]` at `:302`. Component-level coverage at `[AC-185-04c]` (`useDataGridEdit.safe-mode.test.ts:155`), `[AC-185-05c]` (`EditableQueryResultGrid.safe-mode.test.tsx:190`), and Mongo `[AC-188-03e]`.
- [x] **Criterion 7** — `SafeModeToggle` tooltip reflects new policy — `SafeModeToggle.tsx:30-77` body; verified by `[AC-245-T1..T3]` at `SafeModeToggle.test.tsx:81-128`.
- [x] **Criterion 8** — 5 verification checks pass — confirmed by orchestrator + my spot re-runs (3+3 test files passing).

## Feedback for Generator

1. **AC ID pinning — partial gap**:
   - **Current**: Contract enumerated `AC-245-G1`, `AC-245-G2`, `AC-245-G3`, `AC-245-C2`, `AC-245-C5`, `AC-245-H1` as expected test labels. Generator preserved the assertions but kept the historical AC IDs (`[AC-185-06]`, `[AC-185-04a]`, `[AC-231-01a]`, `[AC-231-01c]`, etc.) and only added `AC-245-L*`, `AC-245-N*`, `AC-245-T*`, `AC-245-C1/C3/C4*`, `AC-245-H2`. Result: `git grep "AC-245-G1\b"` returns 0 hits even though the assertion exists.
   - **Expected**: Each contract AC ID should appear as a literal test label so contract-to-test traceability is grep-able.
   - **Suggestion**: In Phase 2 / future sprints, either (a) add the canonical AC IDs as second-line tags in test descriptions (e.g., `it("[AC-185-06 / AC-245-G1] ...")`), or (b) update the contract template to allow Generator to map old ACs onto new IDs without renaming when the assertion is identical. For Phase 1, no action required — every assertion is preserved.

2. **`AC-245-L6` reason-copy interpretation drift**:
   - **Current**: Contract `AC-245-L6` says "off 는 prod-auto copy, strict/warn 은 toolbar override copy". Generator implemented bare analyzer reason for prod+strict|warn (no toolbar override hint appended), citing the Phase 1 dialog's type-to-confirm mechanism + the explicit Invariant "현재 텍스트 그대로 유지".
   - **Expected**: Either the toolbar override copy as the contract literally says, or an explicit acknowledgement that the contract sentence describes the eventual Phase 2 dialog text, not the Phase 1 `action.reason` field.
   - **Suggestion**: Generator's interpretation is defensible (handoff Assumptions §2 explains it well, and `[AC-186-04a]` / `[AC-186-05b]` / `[AC-231-02b]` would break otherwise). Keep as-is for Phase 1; flag in Sprint 246 contract that the override hint must surface as a separate dialog field rather than mutating `action.reason`. ADR 0022 body could also be amended with a clarifying note about which field carries the hint in each phase.

3. **Re-purposed AC IDs (`AC-231-01a`, `AC-231-01c`, `AC-231-02a`, `AC-187-04a`, `AC-187-05a`, `AC-187-06a`, `AC-188-03a`, `AC-188-03e`, `AC-190-01-5`, `AC-235-06`, `AC-236-06`)**:
   - **Current**: These tests previously asserted block flow on prod+strict; under Phase 1 they assert confirm dialog. Generator updated descriptions and assertions consistently, with header comments explaining the inversion (e.g., `useDataGridEdit.safe-mode.test.ts:1-20`, `QueryTab.safe-mode.test.tsx:1-28`).
   - **Expected**: Either the AC IDs are renamed to fresh `AC-245-*` IDs to keep blame archaeology clean, or the descriptions are updated and old assertions are explicitly noted as inverted (Generator chose the latter).
   - **Suggestion**: Generator's choice is defensible — the test scenario family is identical (prod+strict+destructive), only the post-policy expectation flipped. The header comments cite Sprint 245 + ADR 0022 Phase 1 so future reviewers can find the inversion. If a stricter audit trail is desired, future sprints could mandate AC-ID renaming on every assertion inversion (e.g., `[AC-231-01a]` → `[AC-245-CC1]` etc.). For Phase 1, no action required.

4. **`AC-245-G3` (TypeScript error if `readOnly` prop is used)**:
   - **Current**: Verified indirectly — `pnpm tsc --noEmit` passes and the toolbar interface no longer has the prop. No literal `[AC-245-G3]` test pin.
   - **Expected**: Either an `expectError` ts-expect-error fixture, or treat tsc as the verification.
   - **Suggestion**: tsc passing IS the verification — the prop literally cannot be used. No additional test needed. Acceptable as-is.

5. **Unaccounted-for contract scope file count**:
   - **Current**: Contract In Scope listed 5 production files + 7 test files = 12. Actual git diff stat shows 5 production + 18 test = 23 files. Handoff lists 22 (off-by-one — `QueryTab.execution.test.tsx` is mentioned but not separately tallied vs contract in handoff "Test code (12)" header).
   - **Expected**: Generator should explain why the extra 11 test files were touched. Handoff §"Changed Files" does enumerate each one with the rationale, so this is properly disclosed — just not aggregated into the count.
   - **Suggestion**: For Sprint 246+ contracts, allow "structural test fallout" as an explicit scope-extension category (anything that was assuming the previous Sprint 244 read-only block path). For Phase 1, the extra touches are necessary and disclosed — no action required.

## Residual Risk Disposition

The handoff lists 3 residual risks. My read:

1. **Phase 2 dialog redesign tightly coupled to reason-copy semantics** — *acceptable for Phase 2 follow-up*. Sprint 246 contract should explicitly call out the `[AC-186-04a]`, `[AC-186-05b]`, `[AC-231-02b]` typed-confirm tests that need their typed strings updated when Phase 2 surfaces the override hint as a separate dialog field.
2. **Mongo dry-run unsupported** — *acceptable for Phase 3 follow-up*. ADR 0022 already documents this trade-off in §"트레이드오프".
3. **`AC-231-01a / 01c / 02a` re-purposed** — *acceptable for Phase 1*. The handoff transparently lists this; if Phase 2 re-introduces a block path for cancellable destructive, those tests need re-evaluation. Header comments at `QueryTab.safe-mode.test.tsx:166-171, 349-353, 396-401` document the inversion clearly.

None of the 3 risks are blocking for Phase 1 completion.

## TDD Canary Verification

Per `AC-231-06` mandate, one case must demonstrate the previous-version FAIL.

`[AC-231-01a]` (production + strict + WHERE-less DELETE → confirm dialog, executeQuery NOT called) at `QueryTab.safe-mode.test.tsx:172-199` is the canary. Pre-Sprint-245 code under Sprint 244's read-only block path would have called `executeQuery` (or fallen back to a toast block depending on the call-site) — neither matches the new `not.toHaveBeenCalled()` + dialog assertion. The header comment at `:166-171` explicitly notes this as "the TDD canary" and pins the inversion to Sprint 245. Verified.
